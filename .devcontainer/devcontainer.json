{
  "name": "base",
  "build": {
    "context": "..",
    "dockerfile": "./dockerfile"
  },
  "remoteUser": "nishko",
  "workspaceFolder": "/workspaces/resume",
  "features": {
    "./features/zsh": {},
    "./features/neovim": {},
    "./features/node": {}
  },
  "customizations": {
    "vscode": {
      "settings": {},
      "extensions": [
        "vira.vsc-vira-theme",
        "redhat.vscode-yaml",
        "gruntfuggly.todo-tree",
        // Project Specific Application
        "angular.ng-template",
        "ms-azuretools.ms-entra",
        "EditorConfig.EditorConfig"
      ]
    }
  },
  "postCreateCommand": "sudo chown -R $(id -u):$(id -g) /workspaces/resume && sudo chown -R 1000:1000 /home/nishko/.npm",
}